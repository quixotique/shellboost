#!/bin/bash

case $(type -t gvim) in
file)
    exec gvim --nofork "$@"
    exit "cannot execute gvim, trying vim" >&2
    ;;
esac
case $(type -t vim) in
file)
    exec vim "$@"
    exit "cannot execute vim" >&2
    exit 1
    ;;
esac
echo "not found: gvim or vim" >&2
exit 1
